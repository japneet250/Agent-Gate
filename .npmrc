# NOT engine-strict: @google/genai v2 declares node>=20 and we run 18.20.5.
# It works in practice, but turning this on blocks `npm install` for everyone.
# Run `npm run doctor` to see version drift instead. See SHARED_CONTEXT.md.
engine-strict=false
