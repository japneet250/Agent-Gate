node_modules/
dist/
.turbo/
.env
.env.local
*.log
.DS_Store
.wrangler/

# P3 eval reports -- regenerate with a run.
# report.baseline.json is NOT ignored: it is the accepted --model=engine run,
# committed so a fresh clone can render the dashboard with real numbers.
packages/evals/report.json
packages/evals/report.by-model.json
packages/evals/report.failed.json

# Python (P2 engine)
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# local tooling (cloudflared binary)
.tools/

# ---- merged from Person 1's branch ----
*.pyc
.dev.vars

# Demo runtime logs from ./demo.sh
.demo-logs/

# Vercel project link (apps/dashboard is deployed from repo root; see .vercel/project.json)
.vercel
