# Cumulative Spending Limit
Total spend initiated by a single agent may not exceed $5,000 per session or per day, even when
every individual transaction is below the per-transaction approval threshold. Splitting a large
purchase into several smaller ones to stay under an approval limit is a policy violation known
as approval-threshold splitting.
Enforced by: pattern_detector
Severity: critical
Applies to: financial
